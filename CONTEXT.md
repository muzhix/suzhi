# 溯知研究平台

溯知把来源材料组织为可检索、可关联且能回到原文的研究记忆。本文只固定跨产品、领域和界面使用的核心语言。

## Language

**文档（Document）**：
一条可独立管理的书籍、论文、档案、网页快照或其他来源材料记录。
_Avoid_：文献（指平台资源时）

**文档库**：
用户管理文档、版本、内容提取和 EDU 批量抽取的集合与入口。
_Avoid_：文献库

**结构方案（Structure Scheme）**：
一书一份的解析规则：文前目录、标题层级、目录与正文对齐、段落切分。预览确认后才写不可变文档版本。卷、纪、年是目录节点，写入 `text_unit.path`，不是独立文档。
_Avoid_：篇章表、一书一解析器类

**实体（Entity）**：
跨文档和项目复用的身份索引记录，不归属于单一项目，也不表示平台对争议身份作出权威裁定。
_Avoid_：项目实体（指实体所有权时）

**项目关注实体（Project Focus Entities）**：
项目从共享实体中按需标记的软优先候选，可以为空，不排除未被标记的实体。
_Avoid_：项目实体集、项目私有实体池、实体白名单

**文档级 EDU**：
只依赖固定文档版本及其内部上下文形成、可在多个项目中复用的事件式语义单元。
_Avoid_：项目级 EDU（指首期来源语义记录时）

**实体关联图**：
由文档级 EDU 参数和文档级实体链接投影出的可重建查询视图，不是独立事实账本。浏览范围是有权文档版本集合（文档、项目或全局），不另存合并图。
_Avoid_：知识图谱（指平台裁定的客观世界时）、项目私有图

**未链接事件要素**：
尚未链到共享 Entity 的 EDU 参数；图上作为显示节点出现，确认链接前不是实体。
_Avoid_：待处理实体、临时实体

**事件组**：
按共享实体、时间等信号在查询时聚合的一组相关 EDU，用于跨来源浏览，不是合并后的事件身份。
_Avoid_：事件簇（指首期领域对象时）

**文档级实体链接**：
固定文档版本中的 EDU 参数与共享 Entity 身份索引记录之间的连接结论，可被引用该版本的多个项目复用。
_Avoid_：项目实体链接、项目私有实体

**实体链接纠错建议**：
有文档查看权限的用户针对文档级实体链接提出的修正意见；建议本身不改变链接，须由文档编辑者处理。
_Avoid_：待处理实体候选、项目级实体链接覆盖

**主要谓词**：
概括 EDU 主要行为、状态或关系的开放语义标签，不是平台统一裁定的规范关系类型。
_Avoid_：规范谓词、宽关系类型
