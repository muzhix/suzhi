# 图谱与 EDU 关联分析（graph-k3）

> 日期：2026-09-10
> 状态：产品分析稿（未评审）。阶段顺序以 `blueprint.md` 1.15 / `dev-landing.md` 0.5 为准：B0 后先做 B1 全书结构，图谱仍在 C；本文不改实施顺序。
> 输入：`blueprint.md` 1.13、`dev-landing.md` 0.2、B0 代码现状、graphify 开源仓库（Graphify-Labs/graphify）、app.graphify.com 实测（workspace：hanbd，已索引 suzhi 与 inference 两个仓库）、本仓库 `graphify-out/GRAPH_REPORT.md`（graphify CLI 对 suzhi 代码库的实际构建产物）

## 1. 背景与问题

B0 已完成并验收：TXT/Markdown 上传 → 显式内容提取 → 显式 EDU 抽取 → 阅读器对照原文的最小闭环。下一站是阶段 C（实体关联图）。在动工之前，本报告回答三个问题：

1. EDU 与图谱在领域和产品上到底如何关联，阶段 C 的图谱应该长成什么样；
2. graphify 的产品设计（多 workspace、Overview、单仓/跨仓图谱、统计/展示/操作/搜索/属性检查）哪些可以迁移到溯知，哪些不能；
3. 图谱节点与边的设计如何细化，workspace/tenant 的前瞻预留怎么做。

本报告不修改上位方案。涉及与 `blueprint.md` 冲突的建议，都会显式标注"需修订上位方案后生效"。

## 2. 现状基线

### 2.1 B0 已交付的能力

后端（`backend/src/main/java/com/ontotrace/`）：

- `document`（含 `asset`、`parser`）：文档、资产、上传会话、项目、文本单元；
- `semantic`：Edu / EduArgument / EduSourceRef 三表写入，ContextAssembler、SourceLocator、EduValidator、模型网关（live + stub）、复核开关；
- `runcontrol`：数据库 job 队列（领取、租约、重试、幂等）、processing run；
- `retrieval`：`SearchController` 一个文件，`ILIKE` 子串匹配，原文与 EDU 两路并列返回；
- `security`：会话认证、文档 ACL、项目成员、用户管理。

迁移 V002–V006 已建表：`app_user`、`document`、`document_acl`、`project`、`project_member`、`project_document`、`asset`、`upload_session`、`document_version`、`text_unit`、`job`、`processing_run`、`edu`、`edu_source_ref`、`edu_argument`。

前端：登录、文档库（列表/详情/上传/任务进度）、阅读器、检索页、用户管理。侧边栏一级入口只有文档库、检索、用户（管理员）。

### 2.2 图谱相关的现状

**图谱代码为零。** 没有 `entity` 包，没有 `GraphRetrievalStrategy`，没有任何实体表。但 EDU 侧的原料已经齐了，而且质量比预期好：

- `edu_argument` 已经有 `role`（九个固定角色）、`roleName`、`value`（原文写法）、`entityType` 四个字段——这正是蓝图第 10.4 节边模型所需要的全部信息，实体链接落地后每行参数就是一条潜在的边；
- `edu` 已有 `type`（EVENT/STATE/RELATION/EVALUATION/RULE）、`predicate`、`timeValue/timeSourceForm/timePrecision`、`status`（proposed/active/rejected/superseded）、`locationPrecision`、`usedExternalKnowledge`、`reviewResult`——这些字段足以支撑图谱节点检查器里的全部置信与溯源展示；
- `edu_source_ref` 已有 `quote/charStart/charEnd/precision/purpose`——边的"回到原文"能力已经有数据基础。

也就是说：**阶段 C 的图谱不需要为 EDU 补任何字段**，缺的只是 Entity 一侧（`entity`、`entity_name`、`project_entity`、`edu_argument_entity_link`、`entity_link_suggestion` 五张表，见蓝图 18.1）和图查询/图前端。

### 2.3 蓝图已定的图谱设计基线（第 10 章）

重申既有决策，后文分析都建立在这些约束之上：

- 图是 EDU 和实体的**可重建查询视图**，不是独立事实账本（10.1）；
- 核心节点只有 **EDU 和 Entity 两类**，文档版本和文本单元作为过滤属性，不进图（10.3）；
- 边由 EDU 参数和文档级实体链接产生，按九个固定角色（10.4）；首期不做实体间宽关系投影（10.5）；
- 图查询只在项目选定且有权访问的文档版本内运行，默认一到两跳，带预算约束（10.8）；
- 不建通用 `node`/`edge` 表，不部署图数据库（18.2、17.1）。

## 3. graphify 产品拆解

### 3.1 产品结构

app.graphify.com 的信息架构：

```text
Workspace（多工作区，右上角切换）
├── Overview          工作区总览
├── Graphs
│   ├── Single Repository    单仓图谱
│   └── Cross-Repository     跨仓合并图谱
├── Wiki              由图谱生成的可浏览知识库（带置信审计）
├── Reviews / Formal Verification / Contributors
├── Builds            索引构建历史
├── Repositories      仓库管理
├── Memory / Learn / Integrations / MCP
└── 全局搜索 ⌘K + Ask（⌘I，基于图的深度研究问答）
```

单仓图谱页（以 suzhi 为例，1,404 节点 / 2,448 边）的页面结构：

- **顶部**：仓库切换器、社区筛选（"All communities" 多选复选框，每项带语义标签和节点数）、Kind 筛选（节点类型）；
- **统计条**：Nodes / Edges / **Extracted / Inferred / Synthesized / Observed**——后四项是按"边的来源与确定性"统计的，这是整个产品最有辨识度的设计；
- **画布**：力导向布局，节点按社区着色；全屏、适配视图、聚焦透镜（f）、缩放控件；拖拽平移、滚轮缩放、点节点开检查器、可拖动节点；
- **右侧检查器**（未选中节点时是搜索面板）：节点搜索框（支持 `kind: function`、`path: src/`、`prov: inferred` 结构化过滤语法）、快捷过滤片、"Most connected" 高连接节点榜（名称+类型+度数）、"Combine graphs" 跨仓合并入口、图例。

### 3.2 节点检查器（属性查看）的完整形态

以 `CurrentUser` 节点为例，检查器包含四层：

1. **溯源徽章**：`Extracted`，悬停说明"直接读自源码或 AST，确定性最高。溯源只记录它来自哪里，不构成正确性主张"。
2. **属性**：repo、path（文件级定位）、community（可点击，"Document Access and Management, 91 nodes"）、degree（51 = 50 入 + 1 出）。
3. **Blast radius（影响面）**：1/2/3 跳切换，"51 nodes across 14 files could be affected within 2 hops"，附"Change with care"提示与免责说明（"结构性过近似，不是破坏预测，更不是验证结论"），可一键"Show on canvas"高亮。
4. **连接列表**：Outgoing（1）/ Incoming（50）全部列出，每项可点击继续导航。

每个区块都带一句"这是什么、不是什么"的界定文案。这一点与溯知"EDU 不自动等于客观事实"的产品立场高度同构。

### 3.3 数据模型与置信体系

开源版数据模型（`graphify-out/graph.json` 可直接验证）：

- 节点带溯源（file + line）、社区 ID、度数；
- 边带类型（calls/imports/inherits/uses/references/depends_on…）和**置信标签**：EXTRACTED（源码显式存在）/ INFERRED（graphify 推断归一）/ AMBIGUOUS；
- Leiden 社区聚类 + 自动语义命名（"Job Queue Runtime"“EDU Validation"等）；
- 报告层（GRAPH_REPORT.md）输出：God nodes（枢纽）、Surprising connections（跨文件语义关联）、Hyperedges（超边，如"玄武门之变史料集——同一事件的六种史源记述"把六份史料节点聚成一组）、Import cycles、Knowledge gaps（402 个孤立节点）、Suggested questions。

其中 hyperedge 那条特别值得注意：graphify 在 suzhi 仓库上自动把 `doc/玄武门素材` 下的六份史料识别为"同一事件的六种史源记述"并聚合呈现。**这正是蓝图 4.4"事件浏览不要求先把多来源强行合并为一个真实事件"想要的呈现形式**——不合并记录，但聚合成一个可导航的组。

### 3.4 Wiki：图谱的叙事化出口

Wiki 自述："由代码图谱生成的可浏览知识库：子系统、承重抽象、它们的连接方式，每页带置信审计轨迹，随每次索引提交重新生成。" 文章索引按社区组织，每个节点有度数、可跳回图谱画布（`/graph?repo=suzhi&node=CurrentUser`），图谱与 Wiki 双向互跳。

这对溯知的对应物就是蓝图 16.5/16.6 的实体页/事件页：图的邻居视图和叙事页视图是同一份数据的两个出口。

## 4. graphify 与溯知的结构映射

### 4.1 概念映射表

| graphify | 溯知对应物 | 说明 |
| --- | --- | --- |
| Workspace | 平台实例（现阶段单工作区，未来 tenant） | 蓝图 5.3"平台级=单一工作空间内共享的实体池"，多租户预留见第 8 节 |
| Repository（索引单元） | **文档（Document）**，不是 Project | graphify 对一个 repo 做一次 build 生成一份图；溯知对一个文档版本跑一次 EDU 抽取。二者都是"语料→结构化产物"的单元 |
| Build（索引构建） | EDU 抽取任务（job + processing run） | 都有状态、成本、可重跑、历史列表 |
| Commit SHA | Document Version | 都是"输入的不可变锚点"，产物可追溯到锚点 |
| 节点（function/class/file） | Entity 与 EDU | 蓝图 10.3 已定两类核心节点 |
| 边（calls/imports…） | EDU 参数角色边（subject/object/participant…） | 溯知的边必须能回到 EDU 再回到原文 |
| Cross-repo graph | 项目范围内跨文档的实体关联图 | 见 4.2 差异说明 |
| Wiki | 实体页 / 事件页（16.5/16.6） | 图的叙事化出口 |
| Ask（⌘I 深度研究） | 阶段 D 有据问答 | 图谱检索作为问答的召回通道之一 |
| God nodes | 高连接实体（hub entity） | 如"李世民"在玄武门语料中的位置 |
| Communities | 来源文档分组 / 事件簇（首期不做语义聚类） | 见 7.4 |
| Blast radius | 实体链接/EDU 替代的影响面 | 见 6.7 |

用户此前的判断"graphify 的 repo ≈ 溯知的 project"需要修正一处：**repo 更准确的对应物是文档（版本）**，因为 graphify 是"一个语料单元一份图"；而溯知的 project 对应的是 graphify 里"圈选若干语料做跨仓合并视图"这件事。这个修正很重要，它决定了跨文档图在溯知里不需要显式的"Combine graphs"按钮——共享实体池天然把跨文档连接建立好了，**项目选型本身就是 scope，实体链接就是 combine**（蓝图 10.7 跨来源关联的五种信号）。graphify 需要显式合并，是因为它没有跨 repo 的身份归一层；溯知有 Entity 层，这正是两个产品在数据模型上的本质区别。

### 4.2 不能照搬的差异

1. **边的确定性不同**。graphify 的边来自 tree-sitter AST 确定性解析，EXTRACTED 边基本是客观事实；溯知的边来自模型抽取 + 实体链接，天然带不确定性。因此溯知的图谱 UI 必须把置信呈现做成一等公民（边的来源：参数抽取自原文 / 自动链接 / 人工改链；EDU 的状态与定位精度），不能像 graphify 那样只在一个统计条里交代。
2. **图的权威性不同**。graphify 的图就是它的权威产物（尽管可重建）；溯知的图是投影，权威在 EDU、实体链接和原文（蓝图 10.1）。图谱界面不能提供任何"直接改边"的入口，所有修正必须回到 EDU 或链接的编辑流程——graphify 的画布是只读浏览，这一点反而一致，可以直接沿用。
3. **规模与用途不同**。graphify 单仓几千节点全量渲染；溯知按蓝图 10.8 必须带预算约束做局部扩展（一到两跳），全图渲染只发生在小项目或聚合视图上。
4. **导航结构不同**。graphify 把 Graphs 提为一级导航（它是核心卖点）；溯知的一级入口已由蓝图 16.1 固定为五个（文档库/研究项目/阅读与 EDU/实体与事件/问答与研究成果），图谱是"实体与事件"下的视图形态，不应新设一级入口。

## 5. EDU 与图谱的关联

### 5.1 数据关联（既有定论，重申）

链条是：原文（text_unit）→ EDU（语义权威记录）→ 事件参数（edu_argument，九角色）→ 实体链接（edu_argument_entity_link）→ Entity（身份索引）。图边 = 这条链的第三、四步投影。每条边三跳可回原文：边 → EDU → edu_source_ref → text_unit。这对应 graphify 的边 → file:line 两跳回源。**溯源深度是溯知图谱每条边的硬要求，也是与 graphify 交互最值得对标的一点。**

### 5.2 产品关联：图谱是 EDU 的第二界面

B0 之后的现状是：EDU 只有一个消费界面——阅读器右侧面板，按文本单元顺序排列。这等于把一本索引卡片册按原文页码摆出来，跨文档的关联完全不可见。

图谱解决的就是这个：**它是 EDU 脱离原文顺序后的组织界面**。同一个实体在不同文档、不同叙事位置出现的 EDU，通过 Entity 节点聚到一起；用户从图上发现"这六份史料都在讲同一事件"，再点回每条 EDU 看原文。检索（阶段 B3/D）让 EDU 可查，图谱让 EDU 可逛。graphify 的产品验证了"逛"这条路成立：Most connected 榜、社区筛选、节点检查器的连接列表，本质都是"不提问也能发现结构"的入口。

### 5.3 置信呈现：graphify 最大的启发

graphify 把"这条边从哪来、有多确定"做成了显式的产品语言：统计条按 Extracted/Inferred 分列，节点检查器带溯源徽章和"不是正确性主张"的界定文案。

溯知已有的字段完全可以组成一套自己的置信语言，而且比 graphify 更丰富：

| 维度 | 已有字段 | 图谱呈现 |
| --- | --- | --- |
| EDU 采用状态 | `status`（proposed/active/rejected/superseded） | 节点/边徽章，默认视图只显示 active |
| 定位精度 | `locationPrecision`（exact/unit/page/failed） | 溯源徽章，悬停说明精度含义 |
| 是否外部知识 | `usedExternalKnowledge` | 警示徽章（对应批量审核高影响队列） |
| 复核结论 | `reviewResult`、`reviewNotes` | 检查器内展示 |
| 边的来源 | 链接是自动 / 人工改链 / 纠错建议处理（`edu_argument_entity_link` 的审计字段） | 边徽章（linked-auto / linked-manual / unlinked） |

建议把这套徽章体系在 C 阶段就定下来，作为图谱界面的固定视觉词汇，后续 D 阶段问答引用直接复用。

## 6. 图谱产品设计建议

### 6.1 信息架构与导航

维持蓝图 16.1 的五个一级入口不变，在"实体与事件"下组织图谱视图：

```text
实体与事件
├── 图谱            /projects/:projectId/graph（项目范围图，默认视图）
│                  /graph（平台实体池视图，跨项目，按权限过滤）
├── 实体页          /entities/:entityId        （蓝图 16.5，已有设计）
└── 事件页          /events/:eduId             （蓝图 16.6，已有设计）
```

三个视图共享同一份文档级链接数据，只是 scope 不同（蓝图 16.5 末段已定此原则）。图谱页与实体页/事件页双向互跳：图上点 Entity → 检查器 →"查看完整实体页"；实体页内嵌一跳关系小图，点"在图谱中查看"跳回画布。这就是 graphify 的 Wiki ↔ Graph 互跳模式。

### 6.2 图谱页布局（对标 graphify 单仓图谱页)

```text
┌──────────────────────────────────────────────────────┐
│ scope 切换(项目图/实体池图) │ 筛选:实体类型|来源文档|角色|EDU类型 │
│ 统计条: 实体数|EDU数|边数|链接率|自动/人工链接|定位精度分布      │
├──────────────────────────────┬───────────────────────┤
│                              │ 未选中: 节点搜索        │
│         画布(力导向)           │  过滤语法提示           │
│   Entity/EDU 二部图           │  高连接实体榜           │
│   按实体类型或角色着色          │  图例                  │
│   全屏|适配|聚焦|缩放          │ 已选中: 节点检查器       │
│                              │  (见 6.3)             │
└──────────────────────────────┴───────────────────────┘
```

具体交互决策：

- **画布可交互但只读**：拖拽平移、缩放、点选、拖动节点（布局微调，不落库）。不提供在图上改边、改实体的任何入口——修正全部回到阅读器（EDU 编辑）和实体页（改链/纠错建议）。
- **节点渲染预算**：默认一跳扩展，上限建议 500 节点（前端硬顶）；超出时按度数截断并提示"已隐藏 N 个低连接节点，用筛选缩小范围"。蓝图 10.8 的预算约束在前端要有显式反馈，不能静默吞节点。
- **着色维度**：默认按节点大类（Entity 按实体类型：人物/组织/地点/其他；EDU 按 type）。graphify 按社区着色，溯知首期没有语义社区（见 7.4），用类型着色是最有信息量的替代。
- **可访问性**：学习 graphify 的做法——画布是一张带完整 aria 描述的图（"力导向图，节点按类型着色，拖拽平移、滚轮缩放、点击节点检查"），旁边的搜索、筛选、检查器是等价的无障碍路径。这与 dev-landing 9.4 的要求一致。

### 6.3 节点检查器设计

Entity 节点检查器（graphify 检查器的溯知版）：

```text
┌ 李世民                          [人物] ┐
│ 徽章: 链接 23 条参数 · 自动 19 · 人工改链 4 │
│ 属性: 规范名 | 别称(秦王/太宗/…) | 类型      │
│       度数 23 入 / 18 出 | 项目是否关注      │
│ 相关 EDU(按叙事序, 可切时间轴): 列表         │
│ 出边: 参与的 EDU(按角色分组: subject 12…)    │
│ 入边: 指向它的 EDU                           │
│ 相关实体: 共现实体排行                        │
│ 来源分布: 6 份文档, 各多少条                  │
│ 可能重复实体: 候选列表(合并预览入口)           │
│ 操作: 查看完整实体页 | 在图谱中显示一跳邻域     │
└──────────────────────────────────────────┘
```

EDU 节点检查器：

```text
┌ U3「李世民在玄武门射杀建成」   [EVENT] [active] [exact] ┐
│ 自足表述全文                                     │
│ 谓词: 射杀 | 时间: 武德九年六月四日(年精度)         │
│ 参数: subject 李世民 · object 李建成 · location 玄武门│
│ 来源引用: quote + 定位(可点击回原文, 显示精度)        │
│ 复核: reviewResult + notes | processing run 摘要    │
│ 相似事件区: 其他史料的相关 EDU(不自动合并, 蓝图16.6) │
│ 操作: 打开事件页 | 在阅读器中对照原文                │
└───────────────────────────────────────────────────┘
```

两个检查器的共同点：每条信息都可点击下钻，最终都能到原文。这与蓝图 16.5"任何关系点击后进入对应 EDU 和原文"一致。

### 6.4 统计设计

图谱页顶部统计条（对应 graphify 的 Nodes/Edges/Extracted/Inferred/Synthesized/Observed）：

| 统计项 | 来源 | 对应 graphify |
| --- | --- | --- |
| 实体数 / EDU 数 / 边数 | 图查询 | Nodes / Edges |
| 参数链接率（已链接参数 / 全部参数） | 链接表 vs 参数表 | 链接完成度 |
| 自动链接 / 人工改链 / 待处理纠错建议 | 链接审计字段 | Extracted / Inferred 的"按来源分列"思想 |
| 定位精度分布（exact/unit/page） | edu.locationPrecision | — |
| EDU 状态分布（active/proposed/…） | edu.status | — |

这些统计同时就是阶段 C 质量门 11、12 要验收的指标，做成产品界面一举两得：用户看健康度，验收看同一组数。

### 6.5 搜索与筛选

借鉴 graphify 的结构化过滤语法，定义溯知图谱搜索的过滤词：

```text
type:person 唐朝        # 实体类型 + 关键词
doc:旧唐书 参与         # 限定来源文档
role:subject 李世民     # 限定角色
status:proposed         # 只看待采用 EDU
linked:false 河南王     # 只看未链接参数(候选)
```

实现成本可控：这些维度都在已定的表结构里，`type:/doc:/role:` 编译成 SQL 条件即可。全局 ⌘K 搜索可以在 D 阶段问答入口落地时一并考虑，C 阶段先做图谱页内搜索。

### 6.6 Overview 工作台

graphify 的 Overview 由四块组成：图卡片列表（状态+节点数+分支）、工作区统计（索引率/节点数/成功率）、构建趋势（sparkline）、最近构建。溯知的对应物分两层：

- **全局首页（登录后落地页）**：文档库卡片（文档数、最近抽取活动）、EDU/实体总量、抽取任务趋势、待处理复核与纠错建议队列。B0 之后侧边栏只有"文档库/检索"，这个首页可以把分散在文档列表里的任务状态收拢为工作台。
- **项目页 Overview 区**：项目内文档版本数、EDU 覆盖率、实体链接率、关注实体建议（蓝图 16.3 已有的设计直接落在这里）。

两层都复用 6.4 的统计口径，不另造指标。

### 6.7 影响面分析（Blast radius 的溯知版）

graphify 的 Blast radius 回答"改这个节点会波及谁"。溯知有两个对称的场景，而且蓝图已有原则、只差产品化：

1. **实体合并/拆分的影响面**：合并预览（dev-landing 7.3 接口 `merge-preview`）时展示"N 条 EDU 的链接将变更，其中 M 条被 3 个以上成果引用"；
2. **EDU 替代的影响面**：蓝图 4.6 与 16.8 已定"被大量回答或文章引用的 EDU 即将被替代"要进复核队列——产品化为一跳影响列表：受影响 EDU → 引用它的成果（阶段 E 后），并给出"Show on canvas"式的高亮预览。

实现上就是固定跳查询（1–2 跳、带预算），与 6.3 检查器的邻域查询是同一个后端接口。

## 7. 图谱节点与边的设计细化

### 7.1 节点

维持两类核心节点不变（蓝图 10.3），细化前端呈现属性：

| 节点 | 呈现属性 | 来源字段 |
| --- | --- | --- |
| Entity | 规范名、类型、度数（入/出）、关注状态、来源文档数 | `entity`、`entity_name`、`project_entity`、链接表聚合 |
| EDU | 自足表述（截断）、type、status、定位精度、时间 | `edu` 现有字段，零新增 |

文档不作为图节点，但作为**分组与筛选维度**（对应 graphify 的 repo/path 属性）。时间不是节点，是 EDU 属性和图的切换轴（叙事序/时间轴，蓝图 16.5）。

### 7.2 边

边 = EDU 参数 × 实体链接，角色即边类型（九个固定角色）。需要 C 阶段落地前敲定的细化点：

- **未链接参数的呈现**：未链接的参数值（`edu_argument.value`）默认不画边，但在 EDU 检查器和"未链接候选"筛选里可见（`linked:false`），并作为实体冷启动的候选来源（dev-landing 14.5 步骤 1）。这对应 graphify 的 AMBIGUOUS——不丢弃，单独成列。
- **关系型 EDU 的 EDU—EDU 引用**（蓝图 10.6）：首期是否在图上画出？建议首期只在事件页"相似事件区"以列表呈现，画布不画 EDU—EDU 边，避免二部图之外引入第三种边类型。
- **聚合边**：同一对 (EDU, Entity) 只有一条边（多角色时显示主角色，检查器里分角色展开）。画布上 Entity—Entity 之间不直连（蓝图 10.5 的既定决策），共现关系走"实体 → 相关 EDU → 实体"的两跳路径呈现。

### 7.3 溯源标签体系（边的置信语言）

| 标签 | 含义 | 图谱呈现 |
| --- | --- | --- |
| extracted | 参数与来源引用都精确命中原文 | 默认绿系徽章 |
| linked-auto | 链接由自动链接产生 | 边样式区分（实线/虚线） |
| linked-manual | 链接经过人工改链 | 徽章 + 悬停显示操作者与理由 |
| unlinked | 参数未链接实体 | 灰色，仅在专门筛选下可见 |
| degraded | 定位降级（unit/page 级） | 黄系徽章 |

命名可在 C 阶段实施时再定，但"每条边必带其中一个标签"应当成为不变量，与 graphify"每条边必带 EXTRACTED/INFERRED"对齐。

### 7.4 聚类/社区

graphify 的 Leiden 社区 + 自动命名很出彩，但对溯知要分两步走：

- **首期（C 阶段）**：用**来源文档**作为天然分组（graphify 的 repo 维度），加上**事件簇的查询时聚合**——像 graphify-out 报告里"玄武门之变史料集"hyperedge 那样，把共享实体 + 时间重叠的 EDU 聚成"事件组"呈现（蓝图 10.7 的信号、16.6 的相似事件区）。注意 dev-landing 6.1 已定不建 `event_cluster` 表，所以这是查询时聚合视图，不是领域对象。
- **后续（F 阶段候选）**：实体共现图上的语义社区（如 Leiden）作为"主题发现"增强，先提交失败样本与收益评估再立项，符合蓝图第 25 章的增强准入。

## 8. workspace / tenant 前瞻

graphify 的多 workspace 是"每个用户/组织一个空间，空间内 repo 共享图与 Wiki"。溯知当前是单工作区（平台级实体池）。建议的前瞻策略：

1. **租户边界 = 实体池边界**。多租户时每个租户独立实体池（tenant 内共享、租户间完全隔离），与 graphify 的 workspace 模型一致。不建议跨租户共享实体池——文档 ACL 已经是行级权限，实体池再跨租户会把图查询的权限过滤复杂化，收益却存疑。
2. **表结构预留**。C 阶段的 `entity`、`entity_name` 等表直接加 `tenant_id` 列并纳入所有唯一约束（或先以单租户常量值占位）。UUID 主键、应用生成主键的既有决策让这一步成本很低；后补列则要回填全量数据。
3. **Project 不是租户下的一级隔断**。项目共享租户内实体池（既有设计），图的 scope 链条是：tenant →（权限过滤后的）文档版本集合 → 项目选型。C 阶段的图查询 SQL 从第一天就把"scope = 有权文档版本集合"作为必带条件（蓝图 10.8、dev-landing 10.1 已要求），将来加 tenant 只是这个集合多一层边界，查询形态不变。

## 9. 对阶段 C 计划的影响

### 9.1 开发顺序（对照 dev-landing 14.5）

原六步顺序不需要推翻，微调两处：

1. Entity 与名称表、冷启动种子和候选匹配——不变，补加 `tenant_id` 预留；
2. 文档级参数链接、修订控制、改链与纠错建议——不变，链接表审计字段按 7.3 的标签体系设计（谁改的、为什么、之前是什么）；
3. 由 EDU 参数和链接查询图——不变，落地为**图子图查询 API**（见 9.2）；
4. 实体页、事件页和叙事序时间线——不变，**追加图谱页首版**：统计条 + 筛选 + 搜索 + 检查器 + 一跳画布。实体页与图谱页同期做，共享组件（检查器、EDU 卡片、来源引用展示）；
5. 固定跳检索——不变，与图谱邻域查询共用后端接口；
6. 局部 PPR 实验及消融——不变。

画布力导向可视化放在第 4 步末尾而非开头：先用"检查器 + 列表 + 统计"把图的查询价值做出来（这部分全是 shadcn-vue 组件能覆盖的），画布作为增强。这样即使可视化选型遇到波折，也不阻塞实体链接质量这条 C 阶段主线。

### 9.2 新增技术决策点（C 阶段开工前敲定）

| 决策点 | 选项 | 建议 |
| --- | --- | --- |
| 图 API 形态 | 查询时实时 SQL 拼子图 vs 物化投影表（可重建） | 首期实时 SQL（固定跳 JOIN / 递归 CTE，带 LIMIT 预算）；万级文档实测慢再物化，符合 17.1"根据实测瓶颈拆分" |
| 子图契约 | 一次性返回 nodes+edges+stats 快照 vs 分页邻域 | 邻域式：`GET /api/graph/neighborhood?scope=project&entityId=…&hops=1`，画布局部刷新 |
| 前端图渲染 | cytoscape.js / sigma.js / 自绘 canvas | cytoscape.js + fcose 布局起步（社区成熟、布局可控、canvas 渲染满足 500 节点预算）；D-scale 超预算再评估 WebGL 方案 |
| 事件簇聚合 | 查询时 SQL 聚合 vs 前端聚合 | 查询时 SQL（共享实体 + 时间重叠的 GROUP BY），前端只渲染 |

注意架构红线：图渲染库是纯前端展示层选择，不违反"不部署图数据库"的架构原则；`retrieval/graph/` 包按 dev-landing 5.2 预留的 `GraphRetrievalStrategy` 接口不变。

### 9.3 权限测试补充

在 dev-landing 10.1 的权限测试清单上，为图查询追加两条必须进 CI 的用例：

- 无权文档版本的 EDU/链接不出现在图子图查询结果与统计条数字里（先过滤再聚合，不能先聚合再过滤）；
- 平台实体池视图中，实体节点可见但其全部关联 EDU 无权时，该实体不出现（避免通过"实体存在性"泄漏无权内容线索）。

## 10. 不做清单与风险

**不做（首期明确排除）：**

- 图上直接编辑边/实体（修正必须走 EDU 编辑与改链流程）；
- 实体间宽关系快捷边与语义投影（蓝图 10.5 既定，暂不变）;
- 语义社区聚类（Leiden 类）与自动命名（F 阶段候选）;
- 全图无预算渲染、项目全图导出；
- event_cluster 等新领域对象（查询时聚合代替）;
- 跨租户实体池。

**风险与对策：**

| 风险 | 对策 |
| --- | --- |
| 图可视化开发挤占实体链接质量主线 | 9.1 的顺序调整：列表/检查器/统计先行，画布后置且限预算 |
| 万级文档下子图查询变慢 | 首期不物化，预留物化投影路径；D-scale 阶段用实测数据决策 |
| 节点渲染卡顿 | 500 节点硬顶 + 度数截断 + 筛选引导，graphify 同规模（1.4K 节点）证明 canvas 可行，但溯知按预算约束更保守 |
| 置信徽章过多造成视觉噪音 | 徽章分层：节点上一个主徽章，细节悬停/检查器展开；对齐 graphify"一个徽章 + 一句界定文案"的密度 |

## 11. 结论

1. **EDU 与图谱的关系一句话**：EDU 是语义权威记录，图谱是 EDU 脱离原文顺序后的组织界面——让"这六份史料在讲同一件事"从检索命中变成可逛、可看、可下钻的结构。数据上零新增（EDU 侧字段已齐），缺的是 Entity 五张表、图查询和图前端。
2. **graphify 的正确用法**：借它的交互骨架（统计条按来源分列、结构化过滤语法、检查器四层结构、Blast radius、图↔叙事页互跳），不搬它的信息架构（图谱不进一级导航）和它的确定性假设（溯知的每条边都要带置信语言并三跳回原文）。它对溯知最值钱的一条经验是产品气质：**把"这数据从哪来、有多可信"做成显式 UI，而不是藏在文档里**。
3. **对阶段 C 的净影响**：六步顺序微调（图谱页与实体页同期、画布后置），开工前敲定四个技术决策点（图 API、子图契约、渲染库、事件簇聚合方式），实体表加 tenant_id 预留，权限测试补两条图查询用例。不改架构基线，不改上位方案的既有决策。
