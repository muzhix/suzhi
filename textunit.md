# 全书上传、篇章目录与 Text Unit

本文是需求与建议。实施清单以 `dev-landing.md` 14.2 为准。与 `blueprint.md` / `dev-landing.md` 冲突时以设计文档为准。粒度原则见 `pinggu-grok.md`。

已经拍板、本文不再讨论的：

- Document 对应**书**（《旧唐书》《史记》《资治通鉴》），不对应卷。
- 卷、纪、年是文本结构，写入 `text_unit.path`，不建 `chapter` 表，不拆成数百个文档。
- 用户上传的是全书 txt；书内已有卷标记，各书格式不同，但一书之内有迹可循。
- 解析前允许人工指定或调整结构方案，再生成不可变版本。

要补的是：怎样从这些标记得到目录树、Text Unit 和阅读器三栏。

---

## 1. 要达到的效果

1. 上传一部书的 txt，生成一份文档、一个固定版本。
2. 解析后得到该书的章节目录（卷 / 本纪或世家或列传 / 纪 / 年等，按书而异）。
3. 正文拆成可定位的 Text Unit，供引用、局部重抽和 EDU 对齐。
4. 阅读器左侧是目录树，中间是当前节点下的原文，右侧是对应 EDU。点目录只换可见范围，不把整卷糊成一块正文。
5. 结构识别和 unit 切分可配置。一书一套方案，解析前可预览、可改，确认后才落版本。

B0 现状：空行切段，`path` 为 `p1`、`p2`；阅读器左栏是单元列表，没有目录树。不能拿来灌二十四史。

---

## 2. 仍要分开的三层

| 层 | 落在哪 | 例 |
| --- | --- | --- |
| 书 | `document` | 旧唐书 |
| 篇章节点 | `text_unit.path` 的前缀，目录树由此聚合 | `本纪/卷一/高祖`，`卷第一/周纪一/威烈王二十三年` |
| Text Unit | `text_unit` 一行 | 一段正文；编年体里通常是某年下的一段，不是整年 |

抽取窗口仍是 B2 的事：按预算合并连续 unit。目录节点「卷二」可以当作抽取范围（path 前缀），不必把卷二做成一个 unit。

---

## 3. 三部书暴露的结构差异

### 3.1 旧唐书：目录和卷首标题不是同一套字符串

文前目录形如 `卷一 本纪第一`。正文卷首形如 `本纪第一 高祖`：没有「卷一」，多了传主或篇题。

因此不能指望「正文里再现目录那一行」。要用目录里的「本纪第一」去对齐正文标题，再把「卷一」补到 path 上。目录本身有错字和体例不稳（样本里可见「列伟」「列传卷第一百一十」），**目录只能当线索，卷边界以正文标题为准。**

目录块不要当成阅读正文。解析后应标成文前材料，目录树从正文卷首生成，用目录补卷号和校对。

另：正文段落多为换行加全角缩进 `　　`，不一定有空行。B0 的 `\n\s*\n` 会把一卷粘成极少几块。这类 txt 必须按缩进/换行切段，不能只用空行。

### 3.2 史记：目录与正文标题一致

目录与卷首都是 `卷一 五帝本纪第一`。对齐成本低。仍有文前目录重复一遍，解析时要丢掉第一份，从正文第一处卷标题起算。

篇类可从标题读出：本纪、表、书、世家、列传。表、书与纪传的 unit 规则可以不同；表不宜当叙事段来抽 EDU。

### 3.3 资治通鉴：无总目，卷下有纪、年

卷标记：`●卷第一`。其下：`【周纪一】　起著雍摄提格，尽玄黓困敦，凡三十五年。` 再下：`威烈王二十三年` 这类年号标题。王更迭处还有单独的王名行（如「安王」），随后才是元年。年标题并不永远干净。

没有全书目录时，目录树必须从正文标记现场生成。层次建议：

```text
卷第一
  └ 周纪一
       ├ 威烈王二十三年
       ├ 威烈王二十四年
       └ 安王 / 安王元年 …
```

---

## 4. 编年体要不要按年切 Text Unit

**年是目录节点，默认不是一个 Text Unit。**

通鉴一卷里，一年可以极长。样本里「威烈王二十三年」几乎铺开智伯灭亡、臣光论才德、豫让、魏文侯、吴起，按年做成一个 unit，定位和阅读都会退回「整年一块」。这和按卷做成一个 unit 是同一类错误。

更合适的切法：

| | 目录树 | Text Unit |
| --- | --- | --- |
| 纪传体（旧唐书、史记纪传） | 卷 → 篇（本纪/列传…） | 篇内按段 |
| 编年体（通鉴） | 卷 → 纪 → 年 | **年下按段**；很短的年可以只有一两段 |
| 表 | 卷 → 表名 | 行或说明段，不按叙事段抽 EDU |

点目录「威烈王二十三年」，中间栏展示该 path 下的各段，滚动阅读。抽取「这一年」用 path 前缀，内部仍逐段或按窗口合并。

只有年标题下正文短到接近一段时，年和 unit 才看起来重合。不要为了整齐，把长年强行并成一块。

---

## 5. 需求

### 5.1 结构方案（解析前人工指定）

每本书在「提取文档内容」前绑定一份**结构方案**。系统提供可改的预置方案（旧唐书纪传、史记、通鉴编年），也可以从预置复制后改规则。方案随处理运行快照，版本才能复现。

方案至少能表达：

- 文前目录：有/无；如何识别起止；是否只作卷号对照、不进入正文 unit。
- 标题规则：按层级排列的匹配方式（卷、篇类、纪、年、王名行）。一书可以多条，从上到下试。
- 标题与目录的对齐：相同字符串，或「正文无卷号、用篇名回查目录」。
- 正文 unit：空行段 / 全角缩进段 / 两者兼用。
- 邻居边界：组装上下文时不跨卷；编年体默认不跨年（可配置）。
- 哪些节点类型默认不抽 EDU（表、目录、附录）。

不把「让模型读完全书自己分卷」当作默认。全书调用贵、结果不稳、难以复现。规则先行；预览里对不上的标题再人工改规则或点选校正。

### 5.2 解析预览，确认后才写版本

内容提取不要在未确认结构时直接写下不可变版本。流程：

1. 选定结构方案。
2. 试解析：目录树、各节点 unit 数、未识别标题、目录与正文对不上的卷。
3. 人看预览，改方案或改正文标记后再试。
4. 确认后才生成 `document_version` 和 `text_unit`。

预览失败不得静默退化成 `p1`、`p2`。识别率过低就停在预览，让人改方案。

### 5.3 落库

- 每个正文段落一条 `text_unit`，`path` 为从根到当前节点的稳定路径，例如 `本纪/卷一/高祖`、`卷第一/周纪一/威烈王二十三年`。
- 卷标题、纪标题、年标题本身不是阅读段落。它们是树节点。正文第一段继承该节点 path。
- 文前目录、附录若保留，用独立 path（如 `目录`、`附录`），默认不进抽取。
- 仍不建篇章表。目录树由版本内所有 `path` 聚合而成。

### 5.4 阅读器

落实规划 16.4 的三栏，而不是现在的「单元列表 + EDU」：

```text
左：章节目录树（按 path 聚合，可展开卷/纪/年）
中：当前节点下的原文各段（可滚动；选中一段高亮）
右：该段 EDU
```

- 点卷：中间列出该卷全部段，或只显示卷首并提示「展开下一级」。默认进入该卷第一个子节点，避免一次渲染整部旧唐书。
- 点年或篇：中间只渲染该 path 前缀下的 unit。
- URL 保存版本、path、选中 unit，便于分享「旧唐书·太宗上」这一页。
- 窄屏：目录可收起；原文与 EDU 可切换。
- 排版：文言阅读密度（字号、行距、缩进）单独做；读通鉴站点的字体方案见 `doc/dtj-font.md`，不挡结构能力。

当前页用 `CardTitle`「文本单元」当主标题，和「页面内容区不写页面级标题」的约定不一致。改成目录树 + 原文后去掉这块标题。

### 5.5 抽取与任务

- 「抽取本卷 / 本篇 / 本年」= path 前缀，与落地指南 B2 一致。
- Section 上下文用当前 path（书名 + 卷 + 篇或年），不要把整书摘要塞进每一次调用。
- 默认禁止对通鉴、两唐书这类体量一键「全文抽取 EDU」，必须选到卷或更细，或明确勾选并看到预算。

---

## 6. 建议的结构方案形态

给实现用的概念形状，字段名可在编码时调整。不是要用户手写 JSON；界面是选预置、改几条规则、看预览。

```text
结构方案
  名称：通鉴编年 / 旧唐书纪传 / 史记 / 自定义
  目录
    有无
    起止（连续短行且匹配「卷…」直到第一个无法匹配的空行或书名行之后）
    用途：对照卷号 | 忽略
  标题层级（从上到下，先匹配者更深或更先由方案声明）
    卷：  ^●?卷(第?[一二三四五六七八九十百千0-9]+[上下中]?)
    篇：  ^(本纪|世家|列传|表|书|志).+
    纪：  ^【.+纪.+】
    年：  ^.{1,8}(元年|几年|[一二三四五六七八九十]+年)$
  对齐
    正文标题含卷号 → 直接用
    否则用篇名在目录里反查卷号
  段落
    空行切分
    或 换行+全角缩进 视为新段
  跨节点
    新标题结束上一段
```

旧唐书预置要点：目录作卷号表；正文标题用「本纪第一 高祖」匹配目录「卷一 本纪第一」。史记预置要点：目录与正文同形，丢文前副本。通鉴预置要点：无目录；卷、纪、年三层；年下按段切。

预置覆盖这三部即可开工。新书先复制最接近的预置，对着预览改规则，不要先做通用古籍分卷模型。

---

## 7. 和现有规划怎么接

| 决定 | 去向 |
| --- | --- |
| 一书一文档 | 已定，不改 D01 |
| path 承载卷/纪/年，不建篇章表 | 已写入落地指南 B1、蓝图 6.4 / 16.4 |
| 编年体年节点 ≠ 年 unit | 补充 `pinggu-grok.md` 的史书特例 |
| 解析预览后才写版本 | 落地指南 14.2；不回改 B0 无方案时的空行切分 |
| 阅读器三栏目录树 | 落地指南 9.2、蓝图 16.4 |
| 全文抽取要确认预算 | 落地指南 14.2，挂在 EDU 任务 |
| PDF / MinerU / OCR | 阶段 P，排在写作之后；标题映射到 B1 同一套 path |

本文已并入 `dev-landing.md` 0.5 的 B1 与 `blueprint.md` 1.15。B0 之后第一个开工包就是本文：全书 txt、结构方案、目录识别、三栏阅读器。不要先做 MinerU。没有目录树，灌全书没有阅读价值。

B0 无结构方案的 txt（玄武门切片）继续空行切段，path 用顺序号，避免把结构方案做成所有文件的卡点。

---

## 8. 验收时至少要看到

用用户提供的三份全书（或其中连续若干卷的真实切片）：

1. 旧唐书：目录不进正文；「本纪第一 高祖」挂到卷一；左树能进到该卷；中间是段，不是整卷一块。
2. 史记：文前目录不重复进树；「卷一 五帝本纪第一」下有多段正文。
3. 通鉴：无目录也能生成 卷 → 纪 → 年；点「威烈王二十三年」只看见该年各段；该年不能只有一个巨型 unit。
4. 预览能列出未匹配标题；改规则后不必重新上传文件。
5. 确认前数据库没有新的文档版本。

---

## 9. 建议结论

1. 书是 Document，卷/纪/年是目录，段是 Text Unit。编年体按年**导航**，按段**落 unit**。
2. 各书标记不同，用「一书一结构方案 + 解析预览」消化，不要一书一解析器类，也不要默认让模型分卷。
3. 旧唐书必须以正文卷首为边界，用目录反查卷号；通鉴必须现场建树。
4. 阅读器改成目录树 + 当前节点原文 + EDU。一次只渲染当前 path 下的段。
5. 结构方案确认后再写不可变版本。这是全书进库的前提，作为 B1 开工包；PDF、MinerU 与 OCR 放到阶段 P。

---

## 10. 开工时仍可定

- path 字符串的规范写法（是否零填充卷号、是否同时保留「本纪第一」与「高祖」）。
- 通鉴「安王」这类王名行算独立层，还是并进下一年的标题。
- 表、志默认跳过 EDU 的具体规则。
- 预览修正是只改方案重跑，还是允许人在预览树上点选「这是卷标题」。后一种更灵活，可以第二步再做。
