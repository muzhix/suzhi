# Coding Style

1. Java类、方法、参数添加javadoc注释
2. Java类注释添加`@author hanbd`
3. 进行必要、合理的日志输出
4. 前端组件尽量使用shadcn-vue、ai-element-vue原生组件
5. 页面内容区不使用页面级主标题、副标题。页面名称由顶栏面包屑表示，不要在内容区再写一遍「文档库」「检索」等。登录页可以用卡片标题说明当前动作。表格列名、卡片区块名、弹窗标题不属于页面主副标题。
6. Java 依赖注入字段按类型命名，不要用领域名词的复数。`XxxService` 字段叫 `xxxService`，例如 `DocumentService documentService`，不要叫 `documents`。`XxxRepository` 字段叫 `xxxRepo`，例如 `DocumentVersionRepository documentVersionRepo`，不要叫 `versions`。同类字段按类型统一，例如 `JobService` 一律 `jobService`，`TextUnitRepository` 一律 `textUnitRepo`。不要把 `documents`、`versions`、`uploads`、`jobs` 这类复数当注入字段名。


# Writing Style

涉及技术讨论、架构设计、代码说明和开发方案时，使用自然、专业、直接的中文，优先采用中国软件工程、技术方案和正式文档日常使用的技术表达和常见写法。

中文表达要求：

1. 不要将英文句法直接映射为中文。避免过长的前置条件、嵌套从句和连续名词化表达。
2. 优先使用短句和明确的主谓宾结构。一个句子承载的核心逻辑不宜过多，复杂条件应拆开说明。
3. 避免生造抽象词和非常规搭配，例如“延后出口”“决策表面”“未来可能性偷渡进首版”等。
4. 避免为了显得深刻而使用不必要的隐喻、比喻或文学化表达。技术内容优先准确、直接、自然。
5. 少用“进行、实现、形成、产生、参与、具备”等泛化动词堆叠，能直接表达时使用具体动词。
6. 避免英文技术文章常见的抽象主语，例如“真实任务证明……”“差异参与……”“约束驱动……”；优先改写成符合中文习惯的具体表达。
7. 对条件复杂的句子，优先使用“如果……那么……”“只有……才……”或拆成两到三句话，不要把多个条件压缩到一个长句中。
8. 技术术语可以保留专业性，但连接这些术语的句子必须符合自然中文习惯。
9. 写完后自行检查：如果一句话虽然能理解，但不像一个中国资深工程师会自然说出来，就重新改写。
10. 默认风格应接近优秀的中文技术设计文档、架构方案和工程评审材料，而不是英文论文或英文技术博客的直译文体。

核心原则：准确 > 清晰 > 简洁 > 修辞。不要为了“高级感”牺牲中文自然度。
