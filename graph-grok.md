# 图谱与 EDU：B0 之后的产品方向

本文是阶段 B0 完成后的暂停思考，不是实施清单。核心问题只有两个：

1. 图谱和 EDU 在溯知里到底是什么关系。
2. 产品上图谱应该长成什么样，哪些地方可以参考 Graphify，哪些地方不能搬。

文中部分产品决定已拍板（见第 8 节），并已写入 `blueprint.md` 1.14 与 `dev-landing.md` 0.3。与设计文档冲突时以设计文档为准。

结论可以先说：

- EDU 仍然是语义权威；图谱是 EDU 和实体链接的可重建视图。阅读器按原文顺序消费 EDU，图谱是 EDU 脱离原文顺序后的第二界面：检索让 EDU 可查，图谱让 EDU 可逛。
- Graphify 值得学的是把图做成正式浏览界面：统计条、画布、结构化搜索、节点检查器、跳数、图例、图与叙事页互跳。不值得学的是把图当成事实账本，以及 Wiki 生成词条、Reviews、Formal Verification。
- Graphify 的 repo 对应文档版本，不是 project。溯知有共享 Entity 层，跨文档连接在链接完成时就已经存在，不需要 Graphify 那种显式 Combine graphs。全局视图是同一套查询换更大的 scope，不是另建一张图。
- 未链接事件要素作为显示节点上画布，否则冷启动图空。首期不画 EDU–EDU 边。不要新建通用 `node`/`edge` 表。阶段 C 的 EDU 侧不需要加字段。
- 阶段 C 应该有图谱画布，但不新增第六个一级入口。画布是「实体与事件」的一种视图。检查器打开 EDU 或实体，每条边必须能三跳回到原文。

---

## 1. 当前站位

### 1.1 B0 已经落地的语义基础

B0 打通的是「文档 → 文本单元 → EDU → 回原文」，不是图谱。

已经存在、且阶段 C 会直接用上的：

| 对象 | 现状 | 对图谱的含义 |
| --- | --- | --- |
| 文档 / 文档版本 / 文本单元 | 表、权限、阅读器都在 | 图的过滤范围和来源导航 |
| EDU | 类型、自足表述、谓词、时间、状态、处理运行、定位精度、外部知识、复核 | 图的一类核心节点；检查器和置信徽章的字段已齐 |
| EDU 参数 | `role` / `roleName` / `value` / `sourceForm` / `entityType` 已写入 `edu_argument` | 边模型（蓝图 10.4）要的信息已经在，链接落地后每行参数就是一条潜在边 |
| 来源引用 | `quote` / 位置 / 精度 / purpose | 边回到原文的数据基础 |
| 项目 | 表有了（`project`、`project_document`），前端没有入口 | 项目图的 scope |
| 检索 | 原文 + EDU 关键词 | 无图通道 |

**阶段 C 不必为 EDU 补字段。** 缺的是 Entity 一侧（`entity`、`entity_name`、`project_entity`、`edu_argument_entity_link`、`entity_link_suggestion`）和图查询、图前端。

B0 阅读器目前只展示 EDU 文本和状态，不展示角色、谓词、时间、补全。参数已经在库里，界面还没露出。这是后面做图之前必须补的一刀：用户如果在 EDU 卡片上看不到「太宗 / 李世民 / subject」，就不会信任画布上的那条边。

### 1.2 规划里图谱已经定死的部分

这些不要在这次思考里推翻：

- 图是查询视图，不是第二套权威语义（D08、`blueprint.md` 10.1）。
- 核心节点只有 EDU 和 Entity；文档和文本单元先当过滤属性和来源导航（10.3）。
- 边来自 EDU 参数的固定角色和文档级实体链接；首期不建实体间宽关系快捷边（10.4、10.5、D20）。
- 项目只按所选文档版本过滤图，不复制、不覆盖链接（D08、D19）。
- 不建通用 `node`/`edge` 表（18.2）。
- 默认一到两跳；局部 PPR 先做实验（10.8、14.5）。
- 一级入口仍是五个：文档库、研究项目、阅读与 EDU、实体与事件、问答与研究成果（16.1）。

阶段 C 的交付原意也已经写清：共享实体池、文档级链接、实体页、事件页、固定跳。原方案没有把力导向图作为独立产品模块。这正是本次暂停要补的产品缺口：数据模型够用，浏览形态不够用。

### 1.3 本次要补的不是新理论，是产品形态

规划把图谱主要写成检索策略。Graphify 则把图谱写成用户每天打开的工作台。溯知后面如果只有实体页和事件页、没有可操作的图，用户会觉得「有 EDU，没有关系」；如果反过来把图画成独立知识库，又会重新掉进 24.4「图谱重新变复杂」。

需要找到的中间态是：

```text
EDU 卡片能看懂一条语义
实体页能看懂一个人/一地/一物
图画布能看懂一批关系
三者点进去都是同一份 EDU 和同一处原文
```

---

## 2. Graphify 产品解剖

以下基于 2026-09-10 对 [app.graphify.com](https://app.graphify.com) 的实际浏览，工作空间为 `hanbd`，已索引 `muzhix/suzhi`（约 1.4K 节点）和 `muzhix/inference`（约 33.5K 节点）。开源产物见本仓库 `graphify-out/GRAPH_REPORT.md`。

### 2.1 信息架构

左侧是工作空间导航，不是仓库导航。当前能看到的一级模块：

| 模块 | 实际作用 | 和溯知的关系 |
| --- | --- | --- |
| Overview | 工作空间首页：图卡片、构建统计、图密度、最近索引 | 可参考为项目顶区和任务工作台，不当应用首页 |
| Graphs / Single Repository | 单仓库力导向图 | 对应单文档图 |
| Graphs / Cross-Repository | 组合多个已索引仓库后的图；空态要求先 Combine | 溯知不需要这步作业，见 3.3 |
| Wiki | 由代码图生成的知识库，带置信审计，与画布双向跳转 | 学互跳，不学生成人物词条；对应实体页/事件页 |
| Repositories / Builds | 索引对象与构建流水线 | 对应文档抽取任务，不必做成独立产品模块 |
| Reviews / Formal Verification / MCP / Memory | 代码与 agent 平台能力 | 忽略 |

底部有工作空间切换（当前 `hanbd`）。全局搜索在顶栏。图页中部还有 Ask 输入。产品按 **Workspace → Repository → Graph** 分层。

### 2.2 Overview 在做什么

Overview 不是文档列表，而是「图资产仪表盘」：

- Your graphs：每张图一张缩略图，带节点规模和分支。
- Workspace stats：每日构建、构建结果、Graph density（按仓库列出节点量）。
- Recent builds：哪次索引完成了、多少节点、多久以前。
- 顶栏可以按仓库范围和时间窗过滤。

对溯知有用的不是「构建」这个词，而是三种数字：覆盖、密度、新鲜度。B0 之后这些数字散落在文档列表和任务徽章里，需要收拢，但不因此把文档库从首页撤掉。

### 2.3 单仓库图的工作台

`/hanbd/graph/single` 的布局：

```text
顶栏：仓库选择 · 社区过滤 · Kind 过滤
统计条：Nodes / Edges / Extracted / Inferred / Synthesized / Observed
左：力导向画布（缩放、适配、全屏、focus lens）
右：搜索 + 枢纽节点 + Combine graphs + Legend
选中节点后，右侧变成 Node inspector
```

实测 `suzhi` 图：1404 节点、2448 边，Extracted 2333、Inferred 115。图例写得很克制：社区着色是分组不是裁决；实线、虚线、点线表示确定性；节点大小表示连通度，不是正确性。

右侧搜索支持 `kind:function`、`path:src/`、`prov:inferred`。未选中时展示 Most connected。选中节点后检查器分四层：

1. 溯源徽章。悬停说明「只记录来自哪里，不构成正确性主张」。
2. 属性：repo、path、community、degree。
3. Blast radius：1/2/3 跳，并提示 Change with care；可 Show on canvas。
4. Outgoing / Incoming 邻接表，可继续点进去。

每个区块都带一句「这是什么、不是什么」。这与「EDU 不自动等于客观事实」同构，溯知应直接沿用这种语气。

画布本身是只读浏览：可拖拽平移、缩放、点选、拖动节点调布局。Graphify 也不在图上改边。这一点可以直接对齐。

### 2.4 跨仓库图，以及 Graphify 为什么必须 Combine

Cross-Repository 当前是空态：先 Combine graphs，才会出现跨仓库图。

Graphify 必须显式合并，是因为它没有跨 repo 的身份归一层。每个仓库一份图，函数名相同也不等于同一个节点。

溯知有共享 Entity。同一「李世民」在不同文档版本里链到同一条身份记录后，跨文档边已经存在。项目选型或权限范围是过滤，不是合并作业。见 3.3。

### 2.5 出处模型，以及本仓库里的超边

开源引擎和托管产品同一套心智（见 [graphify.com/concepts](https://graphify.com/concepts)）：

- 代码边来自 AST，确定性，标记为 EXTRACTED。
- 文档/配置等到代码的边由模型补，标记为 INFERRED。
- 解析不清的边保留为 AMBIGUOUS，不丢弃。
- 查询是遍历，不是向量相似；答案是带 `file:line` 的路径。

对本仓库跑 graphify 时，`GRAPH_REPORT.md` 里有一条超边：把 `doc/玄武门素材` 下六份史料标成「玄武门之变史料集（同一事件的六种史源记述）」。这正是蓝图 4.4 / 10.7 / 16.6 想要的呈现：不把多来源合并成一个真实事件，但聚成一个可导航的组。阶段 C 用查询时聚合做这件事，不建 `event_cluster` 表。

和溯知最像的三句话：

1. 边必须带出处。
2. 不确定的东西要留下来，并标成不确定。
3. 图是为了走关系，不是为了生成一篇新的说明文。

和溯知最不像的三句话：

1. 仓库是图的生产单元；溯知的生产单元是文档版本。
2. Graphify 的 EXTRACTED 边接近客观事实；溯知的边来自模型抽取加实体链接，置信必须比 Graphify 更显眼，不能只靠一条统计。
3. Graphify 可以全仓渲染几千节点；溯知必须按 10.8 做带预算的局部扩展。

---

## 3. 概念对照：Workspace、Repo、Project、Document

### 3.1 三层比两层更准

| Graphify | 产品感觉上的溯知 | 数据模型上的溯知 | 说明 |
| --- | --- | --- | --- |
| Workspace | 未来的 workspace / tenant | 规划里的「单一工作空间」 | 共享实体池、权限边界 |
| Repository | 项目 | **文档版本** | 被索引、被抽取的单元 |
| Commit SHA | 固定版本 | Document Version | 输入的不可变锚点 |
| Build | 抽取任务 | job + processing run | 状态、成本、可重跑 |
| Single-repo graph | 单项目图 | **单文档图** | 一份固定版本内的 EDU—实体关系 |
| Combine / Cross-repo | 跨项目图 | **项目图或全局图** | 换 scope，不另跑合并 |
| Wiki | 知识库 | 实体页 / 事件页 | 图的叙事出口，与画布互跳 |
| God node | 高频实体 | 度高的 Entity | 只当导航，不当重要性裁决 |
| Hyperedge / Community | 事件组 | 查询时聚合，非领域对象 | 跨来源浏览，不落成事件簇表 |
| Blast radius | 改链/合并影响面 | merge-preview | 实体合并、EDU 替代时必须预览 |

把 project 当成 Graphify 的 repo：抽取、链接、图都会项目化，违反 D18/D19。把 document version 当成 repo：抽取边界正确。

```text
Workspace / Tenant
  └── 共享 Entity 池          ← 跨文档身份，全局图的 join 键
  └── Document（独立资源）
        └── Document Version  ← 对应 Graphify Repo（抽取单元）
              └── EDU / 参数 / 文档级链接
  └── Project                ← 一种 scope，不是另一种图
        └── 所选文档版本的过滤
        └── 可选关注实体（软优先级）
        └── 研究成果
```

### 3.2 Workspace / Tenant 现在怎么处理

规划写得很明确：首版单一工作空间，不引入组织层级（5.3、21.1）。这仍然对。界面上先不做切换。

表结构不要拖到「要做租户的那一天」再加列。阶段 C 的 `entity`、`entity_name` 等表直接加 `tenant_id`（单租户用常量占位）并纳入唯一约束。后补列要回填全量数据，现在加成本低。

图查询从第一天就把「scope = 有权文档版本集合」当成必带条件。以后加租户，只是这个集合多一层边界，查询形态不变。不要把 Entity 再塞进 Project。

```text
今天：平台共享实体池（表上已有 tenant_id 占位）
以后：工作空间共享实体池
始终：项目不拥有实体，只引用
```

不建议跨租户共享实体池。文档 ACL 已经是行级权限，实体池再跨租户会把图查询的权限过滤变复杂，收益存疑。

### 3.3 三种进图范围：文档、项目、全局

产品上提供三种 scope，对应同一套图查询，只换文档版本集合：

| Scope | 文档版本集合 | 典型用途 |
| --- | --- | --- |
| 文档图 | 当前这一份固定版本 | 核对抽取、改链、看单篇内部关系 |
| 项目图 | 项目所选文档版本 | 研究问题，有项目时的默认工作图 |
| 全局图 | 当前用户有权访问的全部文档版本 | 跨项目发现同一实体、对照多来源 |

**全局图基于 Entity 层自动得到，不另做 Combine。**

Graphify 的跨仓图是一次显式合并作业：把若干仓库的节点对上，生成第三张图。溯知不需要这步。

```text
文档级 EDU 参数
  → 链接到共享 Entity
      → 不同文档版本里的 EDU 已经连在同一个身份上
          → 换一个更大的文档版本集合再查一遍
              → 这就是全局图
```

因此：

- 没有「合并图」按钮，也没有「跨项目图构建任务」。
- 不物化第二份 `node`/`edge`。权威仍在 EDU 和文档级链接。
- 全局图不是「实体池里所有 Entity 的点名册」。一个实体如果在当前用户有权的文档里没有任何可见 EDU，这个实体不能出现。否则会通过「实体存在性」泄漏无权材料的线索。
- 全局图仍然是来源材料的关联视图，不是客观世界知识图谱。文案与项目图相同，见 6.6。
- 密度和权限风险都比项目图大。默认仍建议：有项目时进项目图；全局图是明确切换后的范围，必须带预算和截断提示。

蓝图 16.1 没有单独写「平台图」入口。全局图放在「实体与事件」的 scope 切换里，不新开一级导航。这是产品范围扩大，不是改信息架构。

---

## 4. EDU 与图谱的关系

### 4.1 一句话

EDU 回答「这段材料在说什么」；图谱回答「这些说法怎样连在一起」。原文回答「你凭什么这么说」。

B0 之后 EDU 只有一个消费界面：阅读器右侧，按文本单元顺序排列。这等于把索引卡片按原文页码摊开，跨文档关联完全看不见。图谱补的就是这一面：**它是 EDU 脱离原文顺序后的组织界面。** 检索让 EDU 可查，图谱让 EDU 可逛。Most connected、类型筛选、检查器邻接，本质都是「不先提问也能发现结构」。

```text
原文（最终依据）
  → EDU（语义权威，可读、可检索、可修正）
      → 事件要素 + 角色（EDU 的结构，不是第二套记录）
          → 文档级实体链接（宁缺毋错）
              → 实体关联图（可重建查询视图）
```

少了 EDU，图就会退回三元组；少了图，EDU 就只是一堆互不认识的卡片。

### 4.2 为什么图不能从 EDU 里独立出去

规划 10.2 已经写了：先 EDU，再事件要素与角色，再组织为图。工程上 B0 也是一次模型调用同时产出这三样。图边没有自己的生命周期：

- 改 EDU 参数，边必须变。
- 改文档级链接，边必须变。
- 拒绝或替代 EDU，边必须从默认图消失。
- 项目增减文档或切换全局/项目/文档 scope，边的可见性必须变，边的存盘不能变。

所以阶段 C 仍然按 `dev-landing.md` 14.5：先 Entity 和链接，再「由 EDU 参数和链接查询图，不建立通用边表」。画布只是这个查询的一种投影。

### 4.3 图为 EDU 补上的能力

EDU 单独做不好、图做得好的：

1. **跨卡片导航**：从李世民走到「加授中书令」，再走到李建成，不必先想好关键词。
2. **跨文档对照**：同一实体在《旧唐书》和《资治通鉴》里各有哪些 EDU。全局图把这件事从「先选对项目」里解放出来。
3. **检索扩展**：实体 → EDU → 其他实体，这是固定跳的本职。
4. **影响面**：合并两个 Entity、替代一条高引用 EDU 时，检查器里的邻接和跳数就是 merge-preview 的可视化。
5. **跨来源事件组**：共享实体加时间重叠的 EDU，查询时聚成可导航的组，不合并记录。

图做不好、必须退回 EDU 和原文的：

1. 自足表述、否定、模态、转述。
2. 补全说明和定位精度。
3. 矛盾来源并陈（图上两条边看起来像「事实」）。
4. 问答引用。引用终点永远是原文，EDU 只是桥梁（D12）。

### 4.4 每条边三跳回到原文

这是图上每条边的硬要求，不是检查器的附加功能。

```text
边 → EDU → edu_source_ref → text_unit（固定文档版本）
```

Graphify 是两跳：边 → `file:line`。溯知多一跳，因为中间隔着 EDU。界面上：

- 画布点边或点 EDU，检查器立刻给出摘录和定位精度。
- 再一下打开阅读器，高亮对应文本单元。
- 定位失败的 EDU 不能进默认图（已有：不能进 `Active`）。
- 降级定位必须在徽章上可见。

对应规划 18.3 的不变量 5、6、15。

### 4.5 置信必须比 Graphify 更显眼

Graphify 的 EXTRACTED 边来自 AST，接近客观事实，统计条分列一次就够。溯知的边来自模型抽取加实体链接，天然不确定。不能只靠一条统计交代确定性，要做成固定视觉词汇，阶段 D 的问答引用直接复用。

| 维度 | 已有字段 | 图上怎么用 |
| --- | --- | --- |
| EDU 采用状态 | `status` | 默认视图只显示 `active`；`proposed` 用筛选打开 |
| 定位精度 | `locationPrecision` | 徽章；`unit`/`page` 用降级色 |
| 是否外部知识 | `usedExternalKnowledge` | 警示徽章，对应高影响队列 |
| 复核 | `reviewResult`、`reviewNotes` | 检查器内展示，节点上不堆 |
| 边的来源 | 自动链接 / 人工改链 | 实线 vs 带人工标记的实线 |
| 未链接 | 无 `edu_argument_entity_link` | 点线，连到显示节点 |
| 纠错建议 | `entity_link_suggestion` | **不进图**，进统计条和队列 |

画布上的边样式：

| 情况 | 画法 |
| --- | --- |
| 原文直接出现，精确匹配后自动链接 | 实线 |
| 上下文补全后才链接（`value` ≠ `sourceForm`） | 虚线 |
| 人工改链 | 实线 + 人工标记 |
| 未链接事件要素 | 点线，接到 UnlinkedArgument |

Synthesized 在 Graphify 里是「模型从多源拼出来的」。溯知如果在图上出现这种边，就等于开始写传记。不要。

徽章要分层：节点上只留一个主徽章，细节进悬停和检查器。每个区块带一句「这是什么、不是什么」，对齐 Graphify「溯源不是正确性主张」。

### 4.6 跨来源事件组

首期不做 Leiden 社区，也不建事件簇表。用蓝图 10.7 的信号做查询时聚合：

- 共享实体
- 时间重叠
- 地点相同
- 文本向量相似（B3 之后）
- 用户手工关联（可后补）

呈现上对标 Graphify 超边和蓝图 16.6 的相似事件区：一组可展开的 EDU，默认不合并。这是「逛」跨来源材料的入口，不是平台裁定「这是同一历史事件」。

---

## 5. 节点设计需要细化的地方

规划 10.3 的「两类核心节点」仍然对。需要细化的是显示层和过滤层，不是再增加权威对象。

### 5.1 两核、一显、两层过滤

```text
核心节点（持久化、可查询、可引用）
  EDU
  Entity

显示节点（查询时物化，不建表）
  UnlinkedArgument    ← 尚未链接的 edu_argument；上画布

过滤 / 布局层（不是节点）
  Document Version    ← 范围、来源着色、筛选
  事件组              ← 查询时聚合，可导航，不落表
  Time                ← 可选时间轴，不是时间节点网
```

未链接参数上画布。冷启动时共享实体池为空，参数值仍在 `edu_argument` 里。只画已链接 Entity，第一张图会接近空白。Graphify 对 AMBIGUOUS 的处理是留下并标成不确定，这里对齐：

- 未链接参数可见，形状或描边与 Entity 不同。
- 点击后打开的是「待链接事件要素」，不是一个假 Entity。
- 用户确认后才进入共享实体池并形成正式边。
- 同一字符串的多个未链接值可以在检查器里提示「可能是同一人」，但不自动合并。
- 全局图上未链接节点会更多，视觉上必须明显弱于 Entity，避免整张图被灰色点淹没。可用筛选 `linked:false` 单独查看。

这不是第三类领域对象。它只是 `edu_argument` 的图投影。链接完成后，显示节点被 Entity 替换。

### 5.2 EDU 节点不要长成文件节点

EDU 节点在画布上应显示：

- 短标签：主要谓词 + 一到两个角色值，例如「加授 / 李世民 / 中书令」
- 类型徽标：事件、状态、关系、评价、规则
- 来源弱提示：文档短题名

完整自足表述放检查器。否则图上全是句子，力导向没有结构。

Entity 节点标签是规范显示名，副标签是类型。度高的实体自然变大。界面文案要写明：大只表示在当前范围内被提及得多，不是平台认为他更重要。

### 5.3 边

首期只画 EDU → 实体（含未链接显示节点）的二部图：

```text
EDU  --subject|object|participant|location|...-->  Entity
EDU  --同上，点线-->  UnlinkedArgument
```

不画：

- **EDU → EDU**（关系型 EDU 对另一 EDU 的引用）。首期只在事件页相似事件区和 EDU 检查器里用列表呈现。后续若固定跳或浏览明确漏召回，再画 `edu_ref`。
- Entity → Entity 快捷边
- Document → EDU（用过滤和着色）
- 纠错建议中的链接
- Rejected / Superseded EDU

同一对 (EDU, Entity) 画布上只画一条边；多角色时显示主角色，检查器里按角色展开。开放谓词留在 EDU 上，用于检查器和精确筛选，不升级为边类型（D20）。

### 5.4 检查器，以及图与叙事页互跳

不要为图单独做一套属性模式。选中节点后，右侧就是该对象即将存在的页的缩略版：

| 选中 | 检查器内容 | 已有规划 |
| --- | --- | --- |
| Entity | 规范名、别称、是否关注、链接条数（自动/人工）、相关 EDU、按角色分组的出边、相关实体、来源分布、可能重复、合并影响面 | 16.5 |
| EDU | 自足表述、类型、谓词、角色、时间、限定、来源摘录、补全、状态、定位精度、复核、相似事件组 | 16.6 |
| UnlinkedArgument | 原文写法、规范值、建议类型、所在 EDU、候选 Entity | 9.3 |

检查器操作：

- 查看完整实体页 / 事件页
- 在图谱中显示一跳邻域（Show on canvas）
- 在原文中打开（文档版本 + 文本单元高亮）

这就是 Graphify 的 Wiki ↔ Graph 互跳，对应物是实体页/事件页，不是生成一篇人物说明。实体页可以内嵌一跳关系小图，再跳回画布。不学生成「子系统说明」或传记正文。

Blast radius 用在：共享 Entity 合并/拆分、文档级改链、高引用 EDU 即将被替代。与规划 9.4、16.8 是同一件事，换成跳数可视化。检查器的邻域查询和固定跳检索共用后端接口。

### 5.5 仍然不要做的节点

- 时间节点网
- Mention / Claim / Evidence 节点
- 事件簇节点（跨来源事件身份）；事件组只做查询时聚合
- 项目级 EDU 节点
- 通用本体类节点

24.4 仍然有效：新增节点类型前，必须证明它有独立生命周期和查询价值。

---

## 6. 产品呈现

### 6.1 信息架构

阶段 C 的「实体与事件」做成三视图，带 scope 切换：

```text
实体与事件
  ├── 图谱     画布 + 统计 + 搜索 + 检查器
  │            scope: 文档 | 项目 | 全局（有权材料）
  ├── 实体     列表 / 实体页（检查器的放大版）
  └── 事件     EDU 列表 / 事件页
```

从文档详情可以「在图中查看本版本」。从项目进入则默认项目图。全局图在图谱页切换 scope，不新开一级入口。URL 保存：scope、选中节点、跳数、类型过滤、着色模式。

### 6.2 图谱页骨架

```text
顶栏：scope（文档/项目/全局）│ 着色（类型/来源文档）│ 筛选（类型、角色、EDU 类型、链接状态）
统计条：实体数 │ EDU 数 │ 边数 │ 链接率 │ 自动/人工 │ 定位精度 │ 待处理纠错
左：力导向画布（全屏、适配、聚焦透镜、缩放）
右：未选中时为搜索 + 高连接实体 + 图例
    选中时为节点检查器
```

画布可交互但只读：拖拽平移、缩放、点选、拖动节点（布局微调，不落库）。不提供在图上改边、改实体的入口。修正全部回到阅读器（EDU 编辑）和实体页（改链/纠错建议）。

着色两种模式，用户切换：

1. **按类型（默认）**：Entity 按人物/组织/地点等，EDU 按事件/状态/关系等。类型用颜色，形状仍区分 EDU 与 Entity。
2. **按来源文档**：看跨文档对照时用。来源是客观属性，不是裁决。

语义社区（Leiden 类自动命名）不进首期着色，放到第 25 章之后。跨来源「团」用 4.6 的事件组导航，不作为第三种主色。

Focus lens 值得做：按 `f` 把当前节点的一到两跳提出来，其余降噪。历史材料图比代码图更容易糊成一团。

### 6.3 节点渲染预算与无障碍

Graphify 可以全仓画 1.4K 节点。溯知按蓝图 10.8 必须带预算：

- 默认一跳扩展；二跳要明确打开。
- 前端硬顶建议 500 节点。超出按度数截断，并提示「已隐藏 N 个低连接节点，请缩小范围或改筛选」。不能静默丢节点。
- 全局图默认更应守一跳和硬顶。

画布要有完整的 aria 描述。搜索、筛选、检查器、实体页是等价的无障碍路径。这与 `dev-landing.md` 9.4 一致：图不能只靠颜色表达，边框、图标或文字要同时在。

### 6.4 统计条就是质量门的产品化

| 统计项 | 来源 | 对应 Graphify |
| --- | --- | --- |
| 实体数 / EDU 数 / 边数 | 当前 scope 的图查询 | Nodes / Edges |
| 参数链接率 | 链接表 vs 参数表 | 完成度 |
| 自动链接 / 人工改链 / 待处理纠错 | 链接审计 | 按来源分列 |
| 定位精度分布 | `edu.locationPrecision` | 溯知多出来的 |
| EDU 状态分布 | `edu.status` | 溯知多出来的 |

这些数同时是阶段 C 质量门 11、12 的验收口径。用户看健康度，验收看同一组数。统计必须先按有权文档过滤再聚合，不能先全库计数再藏无权项。

### 6.5 搜索

保持双入口，不要过早揉成一个框：

- **检索页**：面向问题和阅读，走原文 / EDU / 实体三通道（阶段 D 补实体和问答）。
- **图内搜索**：面向结构浏览。C 阶段先做页内搜索，全局 ⌘K 放到 D。

图内过滤词：

```text
type:person 唐朝
doc:旧唐书
role:subject 李世民
status:proposed
linked:false
```

这些维度都在已定表结构里，编译成 SQL 条件即可。

### 6.6 Overview 和工作台

不要为 Overview 再开一个一级入口，也不把 Graphify 式图首页当成登录落地页。文档库仍是第一公民。

分两层，统计口径与 6.4 相同：

- **项目页顶区**：纳入的文档版本、EDU 覆盖、链接率、图密度、最近抽取/链接、打开项目图。蓝图 16.3 的关注实体建议也落在这里。
- **任务工作台**：把散落在文档列表里的抽取进度、待复核、纠错建议收拢。可以是文档库的一个视图或顶区，不是新的「图谱首页」。

未来 workspace 首页可以长成任务工作台加图入口，仍然不要取代文档库。

### 6.7 明确不要搬的部分

| Graphify | 为什么不搬 |
| --- | --- |
| 整站深色代码美学作为应用主视觉 | 阅读器需要排版和纸面感；图可以单独用深色画布 |
| Wiki 生成子系统说明 / 人物词条 | 16.5 禁止把实体页写成传记；互跳要学，正文生成不要 |
| 显式 Combine graphs 作业 | Entity 层已经承担跨文档连接 |
| Reviews / Formal Verification | 代码审查产品 |
| MCP / Connect agent 作为主入口 | 研究用户的主路径是读材料和提问 |
| Builds 独立模块 | 抽取任务挂在文档和项目上即可 |
| 无预算全图渲染、项目全图导出 | 权限、密度、误读 |
| 把社区写成 verdict | 与矛盾并陈原则冲突 |

视觉上分成两套界面：

- 阅读面：现有 shadcn-vue 浅色工作台、三栏阅读器、EDU 卡片。
- 图面：深色画布、少量统计、从中心向外展开的邻接。进入图谱视图再切换。

图面固定出现的说明：

- 这是当前范围内来源材料的关联视图，不是客观世界的知识图谱。
- 未链接节点不是新人物，只是尚未确认身份的说法。
- 来自不同文档的边可以同时存在，并不表示平台已裁决哪一条为真。
- 全局范围只包含你有权阅读的材料。

---

## 7. 和后续阶段怎么接

### 7.1 不要跳过 B1–B3 先做画布

图画布依赖三样东西都稳定：

1. EDU 质量足够进 Active（B2）。
2. 角色和参数在阅读器里可见（B0 已有数据，界面未做，建议提前到 B2 质量闭环一起做）。
3. 检索权限过滤已经在 SQL 里（B3）。图扩展必须走同一套有权文档版本范围。

没有这三样，画布只能演示，不能当阶段 C 的验收物。

### 7.2 阶段 C：顺序微调，画布后置

`dev-landing.md` 14.5 的六步不推翻，改两处：

1. Entity 与名称表：补 `tenant_id` 占位并纳入唯一约束。
2. 文档级链接：审计字段按 4.5 的标签设计（谁改的、为什么、之前是什么）。
3. 由 EDU 参数和链接查询图：落成邻域查询 API，不建边表。
4. 实体页、事件页、叙事序时间线，**同期做图谱页的列表/统计/搜索/检查器**；三者共享检查器、EDU 卡片、来源引用组件。
5. 固定跳检索：与图谱邻域查询共用后端。
6. 局部 PPR 实验：不变。

力导向画布放在第 4 步末尾，不放开头。先用 shadcn-vue 把「检查器 + 列表 + 统计」的查询价值做出来。可视化选型卡住，也不能挡实体链接质量这条主线。

验收除质量门 11、12 外：

- 用户能从实体走到 EDU 再回到原文，也能从画布完成同一路径。
- 关注实体为空时画布仍可用（未链接节点在）。
- 文档 / 项目 / 全局三种 scope 都是同一套查询换集合。
- 统计条数字与子图结果都已先按权限过滤。

### 7.3 图查询的两条权限用例，必须进 CI

在 `dev-landing.md` 10.1 清单上追加：

1. 无权文档版本的 EDU 和链接，不能出现在子图里，也不能进入统计条。先过滤再聚合。
2. 某实体在当前 scope 内的全部关联 EDU 都无权时，该实体不能出现。避免通过「实体存在性」泄漏无权内容。全局图尤其容易踩第 2 条。

### 7.4 开工前要敲定、本文不锁死的技术点

| 决策点 | 建议 | 红线 |
| --- | --- | --- |
| 图 API | 首期实时 SQL（JOIN / 递归 CTE + LIMIT）；万级文档实测慢再物化 | 不部署图数据库 |
| 子图契约 | 邻域式：`GET /api/graph/neighborhood?scope=&entityId=&hops=` | 不要一次倒出项目全图 |
| 前端渲染 | 社区成熟的 canvas 方案即可，500 节点预算内 | 纯展示层，不改变 `GraphRetrievalStrategy` |
| 事件组 | 查询时 SQL 聚合，前端只渲染 | 不建 `event_cluster` |

### 7.5 阶段 D 以后

问答仍是「原文 + EDU + 实体」三通道，图只提供候选和解释路径。Ask 可以作为项目内提问入口，回答区必须是引用流。

`edu_ref` 边、Leiden 社区着色、物化投影、查询时按需 EDU，都走第 25 章的引入条件。

---

## 8. 已拍板，以及还开放的点

### 8.1 已拍板

1. **未链接参数上画布**  
   作为显示节点，视觉弱于 Entity。否则宁缺毋错会让图空。

2. **首期不画 EDU–EDU 边**  
   关系型引用和跨来源对照，用事件页相似事件区、EDU 检查器和查询时事件组。后续有明确需要再画 `edu_ref`。

3. **着色两种模式，用户切换，默认按类型**  
   第二种按来源文档。语义社区不进首期着色。

4. **要有全局视图，靠 Entity 层自动得到**  
   同一套查询，scope = 当前用户有权文档版本。不另做 Combine 作业，不物化第二份图。默认仍是项目图（有项目时）或文档图；全局是明确切换。实体在无权 EDU 上必须隐藏。

5. **不新增「图谱」一级导航**  
   放在「实体与事件」下。文档和项目提供入口，scope 在图谱页切换。

6. **Overview 不取代文档库**  
   项目页顶区承担图资产数字；另用任务工作台收拢抽取/复核/纠错。

### 8.2 仍可在开工时定

- 邻域 API 的精确路径和分页。
- 前端图库选型。
- 全局图在空项目、新账号时的空态文案。
- `tenant_id` 列名是否就叫这个（也可以是 `workspace_id`），语义不变：实体池边界，不是项目边界。

---

## 9. 参考

- 平台规划：`blueprint.md` 第 5、7、9、10、14、16、18、23、24、25 章
- 落地指南：`dev-landing.md` 2.3、7.3、9、10.1、14.5
- 术语：`CONTEXT.md`
- 平行分析：`graph-k3.md`（已吸收其置信词汇、三跳回原文、超边事件组、C 内部顺序、权限用例、租户列预留、图↔页互跳；未链接上画布、全局图实现路径与原文不同）
- Graphify 概念：https://graphify.com/concepts
- Graphify 与 RAG：https://graphify.com/vs/rag
- 托管产品实勘：https://app.graphify.com （2026-09-10，工作空间 `hanbd`）
- 本仓库 graphify 产物：`graphify-out/GRAPH_REPORT.md`
