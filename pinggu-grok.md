# Text Unit 怎么划分：卷、本纪、回，还是段？

本文是 B0 之后的评估，不是实施清单。问题只有一个：面对《旧唐书》分卷、《史记》先分本纪/表/书/世家再分篇、《红楼梦》分回，文本单元按什么切，抽取窗口按什么切。

与 `blueprint.md` / `dev-landing.md` 冲突时以设计文档为准。评估结论已写入 `dev-landing.md` 0.5。本文不改 B0 已验收的空行切段实现。

结论可以先说：

- **不要把三件事绑成一刀。** 卷、本纪、回是篇章结构；Text Unit 是稳定定位块；一次模型调用的分析目标是按预算组装的窗口。规划 6.4 和 12.3 已经分开写了，B0 把它们收成了「空行一段 = 一个 unit = 一次抽取」。
- **按段做 Text Unit 仍然合适**，尤其是文言文。一段往往已经有多件事。用户觉得信息量不够，主要是分层上下文没装满：缺传主、缺当前篇章、缺前后段，不是段本身太短。
- **按卷、本纪、回做唯一 Text Unit，并一次性抽取该篇全部 EDU 和正式 Entity，不合适。** 短列传可以一次覆盖成分析目标，但落库的 unit 仍应保持段级，便于定位、阅读和局部重抽。
- **EDU 和正式 Entity 不要绑在同一次调用里。** 一次调用产出 EDU、事件要素和角色（规划 12.2 已定）；正式实体标识由系统在链接阶段生成。一篇本纪抽完，也不等于这个人的实体做完。

---

## 1. 先把三层切开

| 层 | 回答什么 | 典型粒度 | 谁消费 |
| --- | --- | --- | --- |
| 篇章结构 | 现在读的是哪一卷、哪一篇、哪一回 | 《旧唐书》卷二 / 高祖本纪 / 第 33 回 | 目录、Section 上下文、抽取任务范围 |
| Text Unit | 引用和阅读器要钉在哪一块原文 | 段，OCR 则页或栏；无法再切时才是整块 | `edu_source_ref`、左栏导航、全文检索、局部重抽 |
| 分析目标 | 这一次模型主要从哪段原文抽 EDU | 1 到若干连续 unit，必要时带重叠 | `[TARGET]`，可被 Neighbors / Section 包围 |

规划原文已经这样写：

- 6.4：文本单元「可能是篇、章、节、段落……」；**它不是固定词元分块**；模型上下文可以跨越多个单元。
- 12.3：Target 是「当前需要提取的句、段或文本单元」；Neighbors、所属篇章、文档概览另算预算。分析目标过长时先切成多个重叠目标，不压缩目标原文给背景让路。
- 7.4：EDU 的拆分规则按「一项可独立检索的语义」，不按 Text Unit 边界。
- 12.2：一次调用同时返回 EDU、事件要素和角色；**正式实体 ID 由系统生成**，链接在模型调用之后。

因此「按回划分 Text Unit」和「按回发起一次抽取」不是同一个决定。前者改定位粒度，后者改任务范围。可以「抽取本回」，不必「本回变成一个 unit」。

---

## 2. B0 实际在做什么

内容提取把 TXT 按空行切开，写入 `text_unit`，`path` 目前是 `p1`、`p2`，没有卷、本纪、回。

EDU 抽取对每个 unit 调一次模型。`ContextAssembler` 只拼接**前一个** unit 作为 Neighbor，没有所属篇章，没有文档概览，没有后文。

仓库里的玄武门切片，按同一规则切开以后的规模：

| 文件 | 汉字约 | 空行段数 | 段均汉字约 |
| --- | --- | --- | --- |
| 《旧唐书》太宗本纪武德九年摘录 | 270 | 3 | 90 |
| 《旧唐书》巢王元吉 | 1040 | 3 | 350 |
| 《旧唐书》尉迟敬德摘录 | 690 | 3 | 230 |
| 《资治通鉴》武德九年玄武门 | 1770 | 9 | 200 |
| 《旧唐书》隐太子建成摘录 | 3010 | 4 | 750 |

建成这一段均 750 字。文言文里这已经是高密度事件串，不是「信息量太少」的单元。当前真正缺的是：这段属于哪篇传、传主是谁、上一段的纪年有没有断。这些本该由 Section / Neighbors 提供，而不是靠把整篇本纪塞进 Target。

---

## 3. 卷、本纪、回有多长

电子文本和通行统计的量级如下。版本不同会有出入，评估只看数量级。

| 篇章 | 汉字量级 | 内部形态 |
| --- | --- | --- |
| 《史记·高祖本纪》 | 约 0.8–1.1 万 | 一篇本纪，叙事连续 |
| 《旧唐书》卷一高祖本纪 | 约 0.6–1.5 万 | 一卷对应一篇本纪 |
| 《旧唐书》卷二太宗本纪上 | 常到万字以上 | 按年记事，事件极密 |
| 《旧唐书》卷六十四高祖诸子 | 合传，可到万字以上 | 一卷里多人依次出现 |
| 短列传（如巢王元吉全文） | 约 1 千 | 一篇已经接近一次窗口 |
| 《红楼梦》一回 | 通行本平均约 6 千，常见 0.5–0.9 万 | 对话和场面多，事件密度低于本纪 |

《史记》的表以表格为主，书、志里还有制度罗列。这些不是「一篇文章」，不能和本纪、回用同一套「整篇一次抽」。

粗估产出（只为判断一次调用扛不扛得住，不是验收指标）：

| 窗口 | 史书约 EDU 条数 | 白话约 EDU 条数 |
| --- | --- | --- |
| 1 千字短列传 | 8–15 | — |
| 3 千字建成级切片 | 25–40 | — |
| 6 千字一回 | — | 30–50 |
| 1 万字本纪 | 80–120 | — |
| 1.5 万字本纪上 | 120–180 | — |

规划要求结构化、带角色、带来源摘录。一次调用吐出几十上百条 EDU 时，常见失败不是上下文塞不下，而是：**中间事件漏抽、后半段角色串线、JSON 变长后校验失败、定位摘录被改写。** 读得进和抽得全不是一回事。

---

## 4. 按卷 / 本纪 / 回作为唯一 Text Unit，问题在哪

### 4.1 定位降级等于整篇

来源引用的三级是：精确字符 → 文本单元或页面 → 失败。unit 如果是整篇高祖本纪，降级定位就高亮整篇。阅读器右侧 EDU 也无法按段对齐。图上「三跳回原文」会落到一篇万字正文，复核成本接近没有定位。

B0 样本里建成传一段就有多件事。整篇当一个 unit，等于放弃规划里 unit 作为稳定定位基础的职责。

### 4.2 局部重抽和失败恢复变差

现在可以对单个文本单元重抽。整回一个 unit 之后，「这一段抽错了」变成「整回重来」。本纪按年记事，武德九年抽坏不应毁掉武德八年的 EDU。任务租约、超时和丢弃附件也按 unit 计；窗口越大，一次失败丢掉的越多。

### 4.3 一卷不等于一篇文章

《旧唐书》一卷经常是：一篇本纪，或若干人合传，或一篇志。卷六十四这类合传里，传主会切换。模型若把整卷当 Target，后半卷的「帝」「太子」容易接到前半卷的人。

《史记》先分体裁再分篇。表几乎没有连续叙事；硬当文章抽 EDU，会得到大量空角色或假事件。

《红楼梦》回是好的篇章单位，但一回里场面、对话、评价、插叙叠在一起。整回一次抽，EDU 类型会混，边界比「按段 + 回级 Section」更难稳。

### 4.4 上下文预算会被 Target 吃光

12.3 要求完整保留分析目标。1 万字本纪放进 Target 之后，Neighbors 和 Section 没有预算，或者根本不再需要 Neighbors——因为前后文已经在 Target 里。这看起来省事，实际是把「分层隔离」退回「把整篇贴给模型」。

规划写得很明确：目标过长就切重叠窗口，不压缩目标。把本纪做成一个 unit，等于禁止这条退路。

### 4.5 正式 Entity 不能按篇一次做完

即使用户希望「一篇文章同时得到 EDU 和 Entity」，也应拆开看：

1. **事件要素**：可以也应当和 EDU 同一次调用产出（规范值、原文写法、建议类型）。这是 12.2 的默认。
2. **正式 Entity**：跨卷、跨书共享。李世民出现在本纪、建成传、敬德传、《资治通鉴》。按回或按本纪各做一套实体，会立刻打出项目私有实体池，和 D19 冲突。
3. **宁缺毋错**：一篇里的「太宗」「秦王」「世民」应当在**文档版本级链接**里归一，而不是让抽取模型在本纪末尾输出一个实体表。模型没有全局实体库，也没有其他卷的证据。

所以「一次性抽取一篇文章的 EDU 和 Entity」里，合理的部分只是：这一窗口的 EDU 和参数。不合理的部分是：这一窗口产出正式实体身份。

---

## 5. 按段划分是不是太碎？

对现代公文的短段，可能碎。对这批验证材料，通常不碎。

文言文史书的「段」往往是一次换行或一处空行之间的连续叙事，常见 200–800 字，建成摘录能到 750 字。里面已经有任职、谋害、诛杀、时间切换。真正的风险是**一段里事件太多、EDU 拆不够**，不是信息太少。

白话回的段更短、对话更多。单段作为 unit 仍然适合定位；作为唯一 Target 时，确实可能缺少场面上下文。补法是 Neighbors（前后段）和 Section（第 33 回回目），不是把 6000 字整回并成一个 unit。

用户的直觉里有一半是对的：**抽取窗口可以比单个短段更大。** 规划允许 Target 覆盖一组连续文本。这应当表现为「一次调用吃 2–4 个 unit」，而不是「删除段级 unit」。

当前 B0 只看前一段，而且没有篇章层，所以短段会显得裸。这是组装问题，应在 B2 的分层上下文里修，不回改 B0 切分验收。

---

## 6. 三种做法对比

| 做法 | 定位 | 抽取 | 重抽 | 阅读器 | 实体链接 | 评估 |
| --- | --- | --- | --- | --- | --- | --- |
| A. 空行段 = unit = 一次 Target（B0） | 好 | 缺篇章和足够前后文 | 好 | 好 | 无影响 | 切分可留，组装要补 |
| B. 卷 / 本纪 / 回 = 唯一 unit = 一次 Target | 差 | 输入够，输出列表易漏、易串 | 差 | 左栏失效 | 易做成篇级私有实体 | 不采用 |
| C. 段（或小段组）= unit；回 / 本纪 = path 与任务范围；Target = 按预算合并的连续 unit | 好 | 对齐 12.3 | 好 | 好 | 文档级链接 | 建议 |
| D. 固定词元切块，忽略章回 | 中 | 切断事件和对话 | 中 | 目录没了 | 无影响 | 规划已排除 |

C 的意思用材料写出来：

```text
文档：《旧唐书》
  路径：本纪 / 卷二 / 太宗上          ← Section，任务可「抽取本卷」
    unit p12  「八年，加中书令。九年……」
    unit p13  「六月四日，太宗率长孙无忌……」  ← 阅读器和引用钉在这里
    unit p14  「甲子，立为皇太子……」

一次抽取：
  [SECTION] 旧唐书·太宗本纪上；传主李世民；当前叙事在武德年间
  [NEIGHBOR] p12
  [TARGET]   p13（必要时把 p13+p14 合成一个 Target，仍保留两条 unit）
```

《红楼梦》同理：回是 Section，「抽取第 33 回」是任务范围；unit 仍是回内各段。

短列传全文只有一千字：三个 unit 可以合成**一次** Target，但三条 unit 还在。定位、左栏、局部重抽都不丢。

---

## 7. 抽取窗口怎么定，而不是怎么「按篇」

建议把「一次调用吃多大」做成可测参数，不要写进领域对象。

| 材料 | Target 汉字（建议起点） | 说明 |
| --- | --- | --- |
| 文言文纪传 | 800–2500 | 约 1–4 个史书段；超过则重叠切开 |
| 白话章回 | 1500–3500 | 覆盖一个场面，不覆盖整回 |
| 短篇（小于约 2500 字） | 可以一次吃完全文 | unit 仍分段 |
| 表、目录、纯罗列 | 不按文章窗口 | 行或单元格区域做 unit；EDU 抽取可跳过或另规则 |

重叠：相邻 Target 共享一段边界 unit，减少「事在两段之间被拆丢」。12.3 已经要求重叠目标。

单次输出也要有天花板。没有评测前可用工程上限：一次调用采纳的 EDU 超过约 30–40 条，就应视为窗口过大或粒度过粗，切开再抽，而不是继续加长 Target。具体阈值随 B2 金标准调，不写进表结构。

上下文装填顺序保持规划原样：先完整 Target，再前后 Neighbors，再 Section（回目、传主、本卷题名），最后才是很短的文档概览。对史书，Section 里的传主和纪年，往往比把整卷塞进 Target 更值钱。

---

## 8. 和阅读、检索、图谱怎么接

- **阅读器**：左栏按 unit。EDU 仍按句或 unit 聚合（16.4）。unit 若变成整回，右侧卡片会堆在一个目录节点下，无法扫读。
- **全文检索**：命中要能回到可展示的一块原文。unit 过大，召回结果是整篇高亮。
- **图谱**：边三跳落到 `text_unit`。来源着色和「在原文中打开」都依赖这块还够小。
- **任务工作台**：「抽取本回 / 本卷 / 本传」是产品按钮，内部仍遍历该路径下的 unit。这比改 unit 粒度便宜，也符合 D15 的显式范围确认。

path 现在的 `p1` 不够用。后续解析应写入稳定结构路径，例如 `本纪/卷二/太宗上`、`第033回`。这是 B1 结构方案写入 `path` 的事，不是把整篇合成一个 unit。PDF 标题层级改在阶段 P 映射到同一套 path。

---

## 9. 对后续阶段的含义

不回改 B0：空行切段可以继续当默认 TXT 切分。已知缺口按阶段补。

| 阶段 | 和本文的关系 |
| --- | --- |
| B1 | 一书一结构方案、解析预览、卷/纪/年写入 `path`、三栏阅读器；不要为了「一篇一次抽」改成整卷一个 unit |
| B2 | 补 Section 与文档概览；Target 过长则重叠切开；阅读器露出角色。这是「段显得信息不够」的主修点 |
| B3 | 原文向量仍按 unit。unit 保持段级，召回才回得去 |
| C | 实体链接按固定文档版本跑，不按回建实体。图谱导航用 path 过滤，不用把回做成图节点 |
| P | PDF 页作为 OCR 时的 unit 候选；解析器标题映射到已有 path |

需要新表或新领域对象吗？首期不需要。path 已有字段；任务范围已有「全文或单个 unit」，可以扩展成「path 前缀」。不要建 `chapter` 表，除非出现独立权限或独立版本。

---

## 10. 评估结论

1. **按段划分 Text Unit，对这批材料合适。** 信息量问题出在上下文组装，不出在切分过细。
2. **按卷、本纪、回划分唯一 Text Unit，不合适。** 它们应做篇章路径和抽取范围。
3. **短篇文章一次抽完 EDU，可以；整本纪、整回、整卷一次抽完，不作为默认。** 即使一次抽完，unit 仍分段存储。
4. **不要在抽取调用里产出正式 Entity。** 同一次调用只出 EDU 和参数；身份归一在文档级链接。
5. **默认策略采用第 6 节方案 C。** 用评测决定窗口字数，不把章回粒度写进领域模型。

---

## 11. 开工时仍可定的点

- Target 汉字上限、重叠长度、单次 EDU 条数上限：用建成传、太宗本纪摘录、红楼梦一回做对照后再冻。
- 无标题 TXT 的 path：人工目录、启发式标题行，还是先空着只用顺序号。
- 史记表、志的 unit 规则：与叙事正文分开，避免同一套 EDU 提示。

这些不挡 B1 把结构方案写入 path，也不挡 B2 把 Section 补进 `ContextAssembler`。PDF 标题层级不挡 B1。
